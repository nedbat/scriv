# Requirements for test runs.
-c constraints.txt

-r base.txt               # Core dependencies for this package

coverage                  # for measuring coverage
freezegun                 # for mocking datetime
pudb                      # for when we need to debug
pytest-mock               # pytest wrapper around mock
responses                 # mock requests
pyyaml